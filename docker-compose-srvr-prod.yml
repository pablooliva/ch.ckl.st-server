version: '3'
services:
  app-prod:
    build: .
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "0.5"
          memory: 500M
      restart_policy:
        condition: on-failure
    command: npm run prod
    volumes:
      - .:/app
    env_file:
      - ./.env
    ports:
      - "3000:3000"